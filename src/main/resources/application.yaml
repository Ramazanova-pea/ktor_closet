ktor:
    application:
        modules:
            - ru.fanofstars.ApplicationKt.module
    deployment:
      port: 8080
      host: 0.0.0.0
postgres:
  url: "jdbc:postgresql://localhost/default"
  user: username
  password: password
  jwt: asdfghjk
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
